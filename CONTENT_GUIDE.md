# トレカアンテナ 運用ガイド(自動投稿用)

このファイルは、毎日の自動記事生成タスクと週次の棚卸しタスクが必ず読む「運用ルール」です。
セッションは毎回まっさらな状態で起動するため、判断基準はすべてこのファイルと `data/posts.json` の中身に基づいて行うこと。

## 絶対厳守のルール

- 特定のショップ名・会社名・店舗URL・法人名は一切出さない。あくまで独立した第三者の情報メディアとして書く。
- 実在の相場・価格情報を書く場合、断定的な将来予測は避け、「〜の傾向がある」「〜と言われている」など事実と推測を区別するトーンにする。
- 他サイトの文章をそのまま転記しない。要約・独自の切り口で書く。**言い回しだけでなく記事の構成も真似しない。**
- 参照した個人の記事・投稿を名指ししない。意見に触れるときは「使用者からは〜という声がある」のように一般化する。
- 1本の記事だけを見て書かない。複数の情報を突き合わせて、自分の整理として書く。
- 医療・投資助言のような断定はしない(トレカは趣味・コレクション領域であることを踏まえる)。

## サイト情報

- サイト名: トレカアンテナ
- リポジトリ: このリポジトリのルートが GitHub Pages で公開される
- 記事ファイル: `posts/<slug>.html`
- 記事一覧データ: `data/posts.json`(トップページがここを fetch して一覧表示する)
- スタイル: `assets/style.css` を common で読み込む。新しいCSSクラスを増やす場合はこのファイルに追記する
- アクセス解析: 全ページの `</head>` 直前に以下のGoogleアナリティクス(GA4)タグを必ず入れる(`posts/_template.html` にも入っている。既にタグがあれば重複させない)。

```html
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-PGBR3HY40L"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-PGBR3HY40L');
</script>
```

## 扱うタイトルの優先順位(重要)

テーマ配分とは**別の軸**。テーマ(6分類)を満たしていても、タイトルが偏っていては意味がない。

### 主力5タイトル ― ここを厚く書く

- ワンピースカード
- デジモンカードゲーム
- ドラゴンボール フュージョンワールド(DBF)
- ヴァイスシュヴァルツ
- ヴァイスシュヴァルツロゼ

### サブ ― 少量でよいが、あったほうがよい

ポケモンカード、遊戯王などのメジャータイトル。

**読者は複数のタイトルを並行してプレイしていることが多い。** そのため、サブタイトルに触れること自体が
主力タイトルの読者との接点になる。「他タイトル経験者向けの読み替え」「タイトルをまたぐ比較」のような
横断記事は、主力の読者に届く導線として積極的に書いてよい。

### 配分の目安(1日4本)

| 区分 | 目安 |
|---|---|
| 主力5タイトルを明示した記事 | 2本以上 |
| サブタイトル、またはタイトル横断・比較 | 1本 |
| タイトル名を出さない一般論 | **1本以下** |

1日では5タイトル全部に触れられないため、**週単位で回すこと。** 直近7日間の `data/posts.json` を確認し、
7日以内に1本も出ていない主力タイトルがあれば、そのタイトルを最優先で選ぶ。

**一般論を書きすぎないこと。** 「売り時の考え方」「保管環境」のような記事はどのタイトルにも当てはまる代わりに、
検索で具体的な悩みを持つ読者に届きにくい。タイトル名と固有名詞が入った記事のほうが、読者の課題に直接答えられる。

生成前に `data/posts.json` の既存タイトルを確認し、**主力5タイトルのうち記事数が少ないものを優先**すること。

## テーマ(6分類・満遍なく触れる)

| theme code | 表示名 | 内容の方向性 |
|---|---|---|
| souba | 相場・高騰 | 値動き・高騰予想・高額カードランキングなど。断定を避け「傾向」で語る |
| shinpan | 新弾・収録カード | 新パックの注目カード・収録内容の解説 |
| kaitori | 買取・売却の考え方 | 売り時の考え方、相場表の読み方、一般的な買取知識(特定店の宣伝はしない) |
| guide | 初心者ガイド・デッキ構築 | 始め方、スリーブ/ローダー選び、メタデッキ紹介など |
| kanbetsu | 真贋・保管 | 偽物の見分け方、保管・劣化対策 |
| column | コラム・イベント | 大会レポート風コラム、業界動向、読み物 |

1日4本では6テーマ全部に触れられないため、**テーマも週単位で回すこと。** 過去7日間の `data/posts.json` のテーマ内訳を確認し、本数が少ないテーマから順に4つ選ぶ。同じ日に同じテーマを2本書かない。

## 調査(Web参照)のやり方

記事を書く前に、必要に応じて `WebSearch` / `WebFetch` で調べてよい。ただし**何を誰から取るかを使い分ける**こと。

| 取りたいもの | 見に行く先 |
|---|---|
| 発売日・収録内容・効果テキスト・ルール | 公式サイト、公式X。**事実は必ず一次情報から** |
| カードの使用感、デッキの回り方、評価の割れ方 | プレイヤーが書いた記事・投稿。**ここは実体験が一次情報** |
| プレイヤーが実際に使う言葉づかい | 同上。`data/vocab.md` に蓄積する |

事実は公式、評価と使用感はコミュニティ。この線を守れば両方の良いところが取れる。逆に、相場や発売日をコミュニティの
書き込みだけで書くと誤情報を引き継ぐことになる。

調査の必要度はテーマによって違う。毎回全テーマで調べる必要はない。

- `shinpan` / `souba` ― 情報の鮮度が価値。**調べる価値が高い**
- `guide` / `column` ― 実体験と語彙が効く。**調べる価値が高い**
- `kanbetsu` ― 実体験(反り・劣化・保管の失敗)は有用だが、普遍的な内容も多い
- `kaitori` ― 一般的な考え方が中心。調査の必要性は低い

## 語彙と論点の蓄積(重要)

セッションは毎回まっさらな状態で始まるため、**知見はファイルに残さないと失われる**。次の2つを必ず使うこと。

- **`data/vocab.md`** ― プレイヤー用語集。**記事を書く前に必ず読む。** 載っている語は言い換えずにそのまま使う。
  調査中に新しい語に出会ったら末尾に追記する(既存行は消さない)。
- **`data/insights.md`** ― 論点メモ。何が議論になっているかの蓄積。切り口を決めるときに読み、新しい論点を見つけたら追記する。

用語を正しく使うことは、読者からの信頼に直結する。「デッキが安定して動く」ではなく「回る」、「使いどころがない」
ではなく「腐る」と書く。ただし専門用語を並べただけにはせず、初出の語には短い説明を添える。

## 記事1本の構成テンプレート

`posts/<slug>.html` は以下の構成を基本とする(`posts/_template.html`参照)。

1. タイトル(H1) ― 検索されそうな具体的なキーワードを含める
2. 導入(2〜4文) ― 誰のための記事か、何がわかるかを明示
3. 本文(H2見出しで2〜4セクションに分割) ― 具体性を持たせる(架空の一般論で終わらせない。カード名・弾番号・レアリティなど具体的要素を使う)
4. 表(該当する場合) ― 相場ランキングや比較には `<table>` を使う
5. まとめ(3〜5文)
6. 関連記事へのリンク(あれば2〜3本、`../posts/xxx.html`)

metaタグに `description` を必ず設定する(120字前後、検索結果に出る要約)。

## 品質スコア(0〜100点)のつけ方

記事を書き終えたら、以下の観点で自己採点し `qualityScore` として保存する。

- 具体性(0-25点): 実在のカード名・弾・レアリティ・時期など具体的要素があるか。一般論だけなら0-10点
- 構成(0-20点): 導入・本文(見出し分割)・まとめが揃っているか
- 検索意図適合(0-20点): タイトルと内容が想定キーワードに一致しているか。読者が知りたいことに直接答えているか
- 独自性(0-20点): 既存記事(`data/posts.json`)とタイトル・切り口が重複していないか。重複が多いほど減点
- 読みやすさ(0-15点): 文の長さ、専門用語の説明、誤字脱字がないか

70点未満の記事は棚卸し(週次レビュー)で優先的に改善対象にする。

## data/posts.json のスキーマ

```json
{
  "slug": "op16-atari-card-2026-08",
  "title": "記事タイトル",
  "date": "2026-08-16",
  "theme": "shinpan",
  "excerpt": "一覧に出す100字前後の要約",
  "qualityScore": 78,
  "scoreHistory": [{"date": "2026-08-16", "score": 78}],
  "keywords": ["OP16", "決戦の刻", "当たりカード"]
}
```

新しい記事を追加するときは、この配列の先頭または末尾に1件追加する(既存要素は消さない)。棚卸しで既存記事を改善したときは、同じ `slug` の `qualityScore` を更新し、`scoreHistory` に新しいエントリを追記する(古い履歴は消さない)。

## 毎日の自動生成タスクの手順

1. リポジトリを最新化する
2. `data/posts.json` を読み、直近の投稿テーマ内訳と既存タイトルを確認する(重複回避・テーマ配分のため)
3. **`data/vocab.md` と `data/insights.md` を読む**(語彙と論点を頭に入れてから書く)
4. 必要に応じて調査する(「調査(Web参照)のやり方」を参照)。新しい語・論点を得たら各ファイルに追記する
5. 6テーマに配分しながら記事を生成する(1回の実行での本数は指示に従う)
6. 各記事について `posts/<slug>.html` を作成し、`assets/style.css` を読み込む(`posts/_template.html` をコピーして使えばGA4タグも含めて自動的に揃う)
7. `data/posts.json` に新規エントリを追記する(品質スコア込み)
8. `node tools/build-feed.js` を実行し、整合性チェックを通した上で feed.xml / sitemap.xml を生成する
9. コミットしてpushする(コミットメッセージ例: `daily: add N posts (YYYY-MM-DD)`)

## 週次の棚卸し(品質改善)タスクの手順

1. `data/posts.json` から `qualityScore` が低い順に対象記事をピックアップする(目安: 下位20%、または70点未満を優先)
2. `data/vocab.md` を読む。**古い記事は語彙が外れていることが多く、そこが改善の余地になる**
3. 該当する `posts/<slug>.html` を読み、具体性・構成・独自性・語彙の観点で書き直す
4. 書き直したら再採点し、`qualityScore` を更新、`scoreHistory` に追記する
5. `node tools/build-feed.js` を実行してから、コミットしてpushする(コミットメッセージ例: `weekly-review: improve N posts (YYYY-MM-DD)`)

このガイド自体も、運用しながら気づいた改善点があれば追記・更新してよい。
